# CLAUDE.md

See [`AGENTS.md`](AGENTS.md) - the canonical, tool-agnostic operational
guidance for this repo (what this project is, the `C/` sibling
dependency, setup/build/install, development workflow, coding
conventions, testing approach, and the "do not commit without being
asked" working norm). Kept as one file rather than duplicated across
`CLAUDE.md`/`AGENTS.md` so it can't drift out of sync between coding
agents.

Human-facing docs live in [`README.md`](README.md) and `docs/` - don't
duplicate that content here either; if it's missing or wrong there, fix
those files directly.
